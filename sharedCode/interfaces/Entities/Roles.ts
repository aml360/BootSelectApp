// TODO: Docs in english
/** Esta enumeración posee todos los roles que existen en la base de datos */
export enum Roles {
	ADMIN = 'ADMIN',
	PUBLIC = 'PUBLIC',
	GESTOR = 'GESTOR',
}

export enum RoleStatus {
	ACTIVE = 'ACTIVE',
	INACTIVE = 'INACTIVE',
}
